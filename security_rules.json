{
    "rules": {
        "problem":{
              //TODO: Update to only let people edit their own problems. 
              ".read": true,
              ".write": true
        },
        "solution":{
          //    ".read": true,
          //    ".write": true
          "$user_id": {
            // grants write access to the owner of this user account whose uid must exactly match the key ($user_id)
            ".write": "$user_id === auth.uid",
            ".read": "$user_id === auth.uid"
          }
        },
        "solved":{
              ".read": true,
              ".write": true
        },
        "user":{
          "$user_id": {
            // grants write access to the owner of this user account whose uid must exactly match the key ($user_id)
            ".write": "$user_id === auth.uid",
            ".read": true
          }
        },
        "verifyRequest":{
              ".read": true,
              ".write": true
        }
    }
}